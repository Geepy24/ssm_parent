<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	
	<!-- 全局配置参数，根据需要再用 -->
	<!-- <settings>
		打开延迟加载的开关
		<setting name="lazyLoadingEnabled" value="true" />
		将积极加载改为消极加载，即按需加载
		<setting name="aggressiveLazyLoading" value="false" />
		
		开启二级缓存
		<setting name="cacheEnabled" value="true"/>
	
	</settings> -->
	
	<!-- 定义别名 -->
	<typeAliases>
		<!-- 批量别名定义
		指定包名，mybatis自动扫描包中的pojo类，自动地定义别名，别名就是类名，首字母大小写都可以
		 -->
		<package name="cn.geepy.blog.pojo"/>
	</typeAliases>
	
	<!-- 和spring整合后spring中mapper的扫描器可以替代这个功能
		这里就不需要配置了。
		但是，必须遵循：mapper.xml文件与mapper.java文件同名且在同一个目录
	 -->
	<mappers>
		
	</mappers>
</configuration>